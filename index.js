function happyHolidaysTo (name) {
  return "Happy holidays, " + `${name}` + "!";	
}
 	 
 function happyHolidayTo (holiday, name) {	 function happyHolidayTo (holiday, name) {
-  return "Happy " + `${holiday}` + ", " + `${name}` + "!";	+  return `Happy ${holiday}, ${name}!`;
 }	 }
 	 
 function holidayCountdown (holiday, days) {	 function holidayCountdown (holiday, days) {
-  return "It's " + `${days}` + " days until " + `${holiday}` + "!";	+  return `It's ${days} days until ${holiday}!`;
 }// write your code below!
